<%= simple_form_for [ question, @response ], remote: true do |f| %>
  <%= f.error_notification %>
  <%= f.input :description, as: :text, label: false %>
  <%= f.select :school_id, options_for_select(School.all.map{|s|[s.title, s.id]}) %>
  <%= f.button :submit %>
<% end %>
